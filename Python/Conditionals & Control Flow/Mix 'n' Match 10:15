# Use boolean expressions as appropriate on the lines below!

# Make me false!
bool_one = (2 <= 2) and "Alpha" == "Bravo"  # We did this one for you!

# Make me true!
bool_two =  (3 >= 3) and "Alpha" != "Bravo"

# Make me false!
bool_three = (3 != 3) and "Alpha" == "Bravo"

# Make me true!
bool_four = (3 == 3) and "Bravo" == "Bravo"

# Make me true!
bool_five = (3 == 3) and "Alpha" == "Alpha"